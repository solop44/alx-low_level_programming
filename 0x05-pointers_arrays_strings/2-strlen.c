#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - Updates the value of a pointer
 * @s: a string
 * Return: int (Length of string)
*/
int _strlen(char *s)
{
	int length = size_t trlen(s);
	return (length);
}
